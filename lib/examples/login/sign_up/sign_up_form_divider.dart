import 'package:flutter/material.dart';

class SignUpFormDivider extends StatelessWidget {
  const SignUpFormDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 250.0,
      height: 1.0,
      color: Colors.grey[400],
    );
  }
}
